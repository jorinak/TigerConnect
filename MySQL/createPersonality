CREATE DEFINER=`root`@`localhost` PROCEDURE `createPersonality`(user_id INT, scale1 INT, scale2 INT, scale3 INT, scale4 INT, scale5 INT)
BEGIN

INSERT INTO personality
VALUES
(user_id, 1, scale1),
(user_id, 2, scale2),
(user_id, 3, scale3),
(user_id, 4, scale4),
(user_id, 5, scale5);


END
